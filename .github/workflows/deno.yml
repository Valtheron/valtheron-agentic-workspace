name: Deno

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Deno check (stub)
        # This is a Node.js project — no Deno code exists.
        # This stub satisfies the "Deno / test" required status check
        # that was configured in branch protection when the Deno workflow
        # was first added. It must remain here or the branch protection
        # will permanently block all PRs (0/4 required checks passing).
        run: echo "Node.js project — Deno check not applicable, passing stub."
